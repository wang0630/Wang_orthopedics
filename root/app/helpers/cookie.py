from collections import deque

def test_and_set_cookie(res, cookie, id):
  # cookie_queue = deque(cookie.split('&'), 5)
  # new_cookie = None
  # if id in cookie_queue:
  #   pass
  # else:
  #   if (len(cookie_queue) >= 5):
  #     pass
  #   else:
  #     cookie_queue.append(id)
  #     new_cookie = cookie_queue.join('&')
  # res.set_cookie('recent_view', new_cookie, max_age=86400)
  pass